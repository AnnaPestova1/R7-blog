<p style="color: green"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @post.body %>
</p>

<div id="comments">
 <% if @post.comments.present? %>
    <h1>Comments</h1>
    <%= render @post.comments %>
  <% end %>
  <h2>Add a Comment</h2>
   <%= render partial: "comments/form", locals: {comment: @post.comments.new} %>
  
                      <!--
                      render partial: "comments/comment", locals: {comment: comment} 
                      render partial: "comments/comment", collection: @post.comments, as: :comment
                      render partial: "comments/comment", collection: @post.comments
                      The `comments` method returns an array-like collection, so we can call `each` on it: -->
                      <!--
                      we can loop through a collection and render a partial for each using render's collection: argument
                      <% @post.comments.each do |comment| %>
                       
                        <!-- we'll move this code in partiald (view/comments/_comment.html.erb)
                        <div>
                          <strong><%= comment.name %> says:</strong>
                          <%= comment.content %>
                        </div>
                        -->
  <% end %>
</div>


<%= link_to 'Edit', edit_post_path(@post) %>
<%= link_to 'Back', posts_path %>
